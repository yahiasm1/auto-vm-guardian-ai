import { supabase } from '@/integrations/supabase/client';

// This script is used to create a default admin user in the database
// Since we've migrated to a PostgreSQL database, we'll provide a placeholder
// for this script.

console.log('This script is now obsolete with the PostgreSQL setup.');
console.log('Default admin users are created in the server\'s database.js file.');

// We now set up default users in server/db/database.js during database initialization
