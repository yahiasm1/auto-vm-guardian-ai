
const { app } = require('./index');

// This file is kept for backward compatibility
// The main application is now configured in index.js
console.log('Server started via server.js entry point');

module.exports = app;
